<%- @url = controller.action_name.eql?('new') ? collection_items_path : collection_item_path(@collection_item) %>
<%= form_with(model: collection_item, url: @url, local: true) do |form| %>
  <% if collection_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(collection_item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
        <% collection_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
